{
  $schema: 'https://docs.renovatebot.com/renovate-schema.json',
  extends: [
    'config:recommended',
    'group:monorepos',
    'group:recommended',
    'docker:pinDigests',
    'helpers:pinGitHubActionDigests',
    'replacements:all',
    'workarounds:all',
    ':configMigration',
    'pinDependencies',
    ':pinDevDependencies',
    ':dependencyDashboard',
  ],
  lockFileMaintenance: {
    enabled: true,
    extends: [
      'group:all',
    ],
  },
  commitMessageAction: '⬆️',
  packageRules: [
    {
      matchManagers: [
        'github-actions',
      ],
      commitMessagePrefix: 'ci(dependencies): ',
      commitMessageTopic: '{{depName}}',
      labels: [
        'dependencies',
        'ci',
      ],
    },
    {
      matchManagers: [
        'dockerfile',
      ],
      commitMessagePrefix: 'build(container): ',
      commitMessageTopic: '{{depName}}',
      labels: [
        'dependencies',
        'container',
      ],
    },
    {
      matchManagers: [
        'pep621',
      ],
      matchFileNames: [
        'pyproject.toml',
      ],
      commitMessagePrefix: 'build(dependencies): ',
      commitMessageTopic: '{{depName}}',
      labels: [
        'dependencies',
      ],
    },
    {
      matchManagers: [
        'custom.regex',
      ],
      matchFileNames: [
        'uv.lock',
      ],
      commitMessagePrefix: 'build(lockfile): ',
      commitMessageTopic: '{{depName}}',
      labels: [
        'dependencies',
      ],
    },
    {
      "matchDepTypes": [
        "dependencies"
      ],
      commitMessagePrefix: 'build(dependencies): ',
      commitMessageAction: '📌',
      labels: [
        'dependencies',
      ],
    }
  ],
}
